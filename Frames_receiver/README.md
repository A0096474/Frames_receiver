# Frames_receiver workspace

This workspace was created by the assistant. It contains the workspace file and basic project scaffolding.

Files added:
- Frames_receiver.code-workspace
- .gitignore

Next steps:
- Open the workspace in VS Code: `code Frames_receiver.code-workspace`
